{{!-- 
<div class="container">
{{>profileForm avatarPath=avatarPath fields=fields buttons=buttons name=name}}
  {{#> formModal isOpen=true}}
    <h1>Загрузите файл</h1>
      <input type="file" id="real-input" class="file-input">
   
    {{>button variant='primary' text='Применить'}}
  {{/formModal}}
</div> --}}
{{! pages/editProfile.hbs }}
<div class="container">
{{>profileForm avatarPath=avatarPath fields=fields buttons=buttons name=name}}
  {{#> formModal}}
    <h1>Загрузите файл</h1>
      <input type="file" id="real-input" class="file-input">
   
    {{>button variant='primary' text='Применить'}}
  {{/formModal}}
</div>