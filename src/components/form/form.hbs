<form class="form">
    <h1>{{title}}</h1>
    
    {{#each fields}}
        <div class="form-field">
            {{> input }}
        </div>
    {{/each}}
    
    <div class="form-actions">
        {{#each buttons}}
            {{> button }}
        {{/each}}
    </div>
</form>